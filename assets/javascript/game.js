$(document).ready(function (){

    var gamestate = "start";
    var player;

    //function to build character objects
    function character(name, HP, ATK, restImg) {
        this.name = name;
        this.HP = HP;
        this.ATK = ATK;
        this.restImg = restImg;
      }

      //create character objects
      var wizard = new character("wizard", 10, 6, "../images/wizard.jpg")















})